// @ts-check

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  // Дерево сайдбара строится автоматически из структуры папок в `docs/`,
  // порядок задаётся `_category_.json` (для папок) и `sidebar_position` (для страниц).
  docsSidebar: [{ type: 'autogenerated', dirName: '.' }],
};

module.exports = sidebars;
