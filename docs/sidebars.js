// @ts-check

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  // Sidebar tree is generated automatically from the folder structure under `docs/`,
  // ordered via `_category_.json` (folders) and `sidebar_position` front matter (pages).
  docsSidebar: [{ type: 'autogenerated', dirName: '.' }],
};

module.exports = sidebars;
