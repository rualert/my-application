# Google OAuth 2.0 Client ID (Web application) из Google Cloud Console.
# Не секрет — используется на клиенте, но конкретное значение зависит от
# окружения, поэтому не коммитится (см. .gitignore). Скопируйте этот файл
# в .env и подставьте свой идентификатор.
VITE_GOOGLE_CLIENT_ID=
